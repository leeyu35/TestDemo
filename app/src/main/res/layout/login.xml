<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="300dp"
    android:layout_height="186.5dp"
    android:layout_gravity="center"
    android:layout_margin="37.5dp"
    android:background="@drawable/trade_text_input_bg"
    android:orientation="vertical"
    android:paddingTop="18dp"
    android:focusable="true"

    android:focusableInTouchMode="true"
    android:paddingBottom="18dp">

    <TextView
        android:id="@+id/tv_title"
        android:layout_width="match_parent"
        android:layout_height="20dp"
        android:layout_marginLeft="18dp"
        android:layout_marginRight="18dp"
        android:text="请输入您的内容"
        android:textColor="#111111"
        android:textSize="14dp" />


    <EditText
        android:id="@+id/et_input"
        android:layout_width="match_parent"
        android:layout_height="38dp"
        android:layout_marginLeft="15dp"
        android:layout_marginTop="17dp"
        android:layout_marginRight="15dp"
        android:background="@drawable/trade_text_input_selector"
        android:textColor="#ff000000"
        android:textSize="17dp"
        tools:hint="修改购物车数量" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="35.5dp"
        android:orientation="horizontal"
        android:paddingLeft="12dp"
        android:paddingRight="12dp">

        <TextView
            android:id="@+id/tv_cancel"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:background="@drawable/trade_text_btn_bg"
            android:gravity="center_vertical|center_horizontal"
            android:layout_marginRight="12dp"
            android:text="取消"
            android:textColor="#FFFF5000"
            android:textSize="14dp" />

        <TextView
            android:id="@+id/tv_confirm"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:background="@drawable/trade_text_btn_bg"
            android:gravity="center_vertical|center_horizontal"
            android:text="确认"
            android:textColor="#FFFF5000"
            android:textSize="14dp" />

    </LinearLayout>

</LinearLayout>